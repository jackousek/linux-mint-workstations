---
ansible_user: ansible
pokladna_password_hash: "{{ vault_pokladna_password_hash }}"

# Database credentials stay in the existing encrypted vault.yml.
postgresql_users: "{{ vault_postgresql_users | default([]) }}"
